{% extends '@theme/layout.html.twig' %}

{% set bestsellers = shop.settings.best_sellers_taxon %}

{% block content %}
    <div class="row">
        <div class="col-lg-3">
            <div class="category">
                <div class="title">{{ bestsellers.name }}</div>
                {% for product in bestsellers.products %}
                    <div class="item">
                        <img src="http://www.curesupportwebshop.nl/Files/5/18000/18518/ProductPhotos/Large/211242813.jpg"
                             class="thumb" height="72"/>
                        <div class="info">
                            <a href="{{ path(product) }}" class="title">{{ product.name }}</a>
                            <span class="price">&euro; 39,99</span>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
        <div class="col-lg-9">
            {% block right_content %}{% endblock %}
        </div>
    </div>
{% endblock %}
